---
description: Implement an approved slice with so2x-flow and close with verification.
allowed-tools: Bash(python3 .workflow/scripts/execute.py:*),Read,Write,Edit
argument-hint: [request]
---

Run `python3 .workflow/scripts/execute.py feature "$ARGUMENTS"`.

Requirements:
1. Confirm `.workflow/scripts/execute.py` exists first.
2. Use the approved plan/task context; do not re-plan here.
3. Summarize the saved artifact and verification result briefly.
