# flow commands

This directory contains lightweight command docs for the so2x-flow workflow.

Commands:
- `flow-init`
- `flow-feature`
- `flow-qa`
- `flow-review`
- `flow-plan`

These command docs should describe behavior and outputs, not contain heavy orchestration logic.
Actual orchestration belongs in `scripts/execute.py`.
